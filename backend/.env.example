# -----------------------------------------------------------------------------
# Local development environment for SeatCheck
# -----------------------------------------------------------------------------

# -------------------------------
# Frontend (Expo / React Native)
# -------------------------------
# The FastAPI backend base URL that the Expo web client uses
EXPO_PUBLIC_API_BASE=http://localhost:8000

# The local web address where your Expo app runs
APP_BASE=http://localhost:8081

# -------------------------------
# Backend (FastAPI)
# -------------------------------

# Secret key used to sign session cookies (change in production!)
SESSION_SECRET=dev-insecure-change-me

# Enable local developer login route (/auth/dev/login)
DEV_AUTH=1

# Database connection string for PostgreSQL + PostGIS
DATABASE_URL=postgresql+psycopg2://seatcheck:seatcheck@localhost:5432/seatcheck

# CAS base URL (for when Yale CAS login is fixed)
# CAS_BASE=https://secure.its.yale.edu/cas
